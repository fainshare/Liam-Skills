---
name: osborn
display_name: "太虚一转 · 散怀（奥斯本）"
visibility: public
description: >
  发散思维（太虚一转·散怀）：想法量、SCAMPER、Stakeholder轮转。触发词: 发散, 开脑洞, 奥斯本.
  Divergent thinking (太虚一转·散怀): volume of ideas, SCAMPER, stakeholder rotation → idea-pool.md. Use
  when user says "发散", "开脑洞", "奥斯本", "osborn".
  即便用户未明确说"用 osborn"，当对话出现以下信号时也应主动建议使用：方案只有一条路想扩空间、
  团队讨论在同一批想法里打转、方向定了但觉得可能有更好的选择、用户说"还有别的思路吗""帮我想想还有什么可能"。
  不要等用户说出触发词才启动——识别意图比匹配关键词更重要。
version: "2.1.1"
last_updated: "2026-06-25"
triggers:
  - osborn
  - 奥斯本
  - 开脑洞
  - 开天眼
  - 发散
  - 发散一下
  - 发散思路
  - brainstorm
  - 头脑风暴
  - 创意发散
  - 发散思维
  - 多想几个方案
  - 还有别的角度吗
  - 展开想想
  - 思路打开
  - 脑暴一下
  - 太虚一转
  - 散怀
  - /osborn
  - 帮我想想
  - 还能怎么做
allowed-tools:
  - Write     # 写入 idea-pool.md
  - Read      # 读取已有想法池或上下文文件
  - Bash      # 日期获取、目录创建等辅助操作
compatibility:
  runtime: claude-code    # 依赖 Claude Code skill 加载机制
  filesystem: required    # 需要写入 idea-pool.md 到本地文件系统
  network: not-required   # 纯离线对话协议，不需要网络访问
metadata:
  visibility: public
  stage: 达芬奇（四阶认知验证流程第一阶段）
  hat: green
  risk: low
  capabilities:
    - filesystem-write
  status: V2.1（问题空间覆盖率替代边际贡献+维度轴中途调整+V2.0固化）
---

# Osborn 奥斯本发散

> *"It is easier to tone down a wild idea than to think up a new one."*
> — Alex Osborn

## 概述

方案只想到一条路？方向定了但觉得可能有更好的选择？脑子里的东西倒不出来？团队讨论总是在同一批想法里打转？

先铺量，后筛选。围绕主题做大量发散，不评判、不筛选，把可能性空间彻底铺开。四条核心手法：量级驱动，想法够多好方向才会浮现；延迟判断，"不切实际"的先留着；SCAMPER 七算子补盲，替、合、调、改、用、消、重，逐一扫描思维盲区；利益相关方视角轮换，从用户、对手、新人、老板不同位置看同一件事。产出 idea-pool.md，结构化整理所有想法，直接喂给黑格尔（澄源）做收敛。

太虚五转第一转：散怀（发散）。先把空间打开，后面逐步收紧。

## 这个 skill 做什么

主题摆在你面前，第一个冒出来的想法往往就把后面的可能性挤掉了，这叫**收敛过早**。这个 skill 的工作是**强行把空间先铺开**：不评判、不筛选、不排序，先把数量堆出来，再用 SCAMPER 和视角轮换把盲区补齐。

奥斯本的原话是：把一个野想法调温和，比凭空想出一个新想法容易。这个 skill 信这条。

**核心机制**：把问题空间想象成一张地图，维度轴是坐标轴（如"输入源/处理管线/输出形态"），覆盖率是探索进度。暂停点展示哪些维度已饱和（🔴）、哪些还空白（🟢），驱动"继续发散还是进入下一阶段"的决策。

四个 skill 的分工：**osborn**（🟢发散）→ **hegel**（🔵收敛）→ **bayes**（⚪验证）→ **feynman**（⚫审查）→ **debono**（🟡回溯）。详见 [references/comparisons.md](references/comparisons.md)。

## 与 ce-brainstorm 的区别

两者**完全解耦**：ce-brainstorm 是需求澄清工具（模糊输入 → 结构化需求），osborn 是思维发散工具（明确主题 → 想法池）。需求不清先用 ce-brainstorm，主题已定要扩空间用 osborn。详细对比见 [references/comparisons.md](references/comparisons.md)。

## 六项核心能力

| # | 能力 | 在哪用 |
|---|------|-------|
| 1 | 延迟判断引擎 | P1/P2 不做任何价值筛选、可行性判断、优先级排序 |
| 2 | 量级驱动 | P1 追求数量，相信"数量产生质量" |
| 3 | SCAMPER 七算子 | P2a 结构化补盲 |
| 4 | 想法组合（Hitchhiking） | P1 中 AI 主动在已有想法基础上衍生新想法 |
| 5 | 疯狂想法保护 | 不因"不切实际"而过滤，调温和比凭空想新的容易 |
| 6 | 发散-收敛双阶段协议 | 本 skill 只做发散，收敛交给 hegel |

## 执行流程：P0 → P1 → P2(a+b) → P3

```
P0 明确主题 → P1 自由发散 → P2a SCAMPER 补盲 → P2b 视角轮换 → P3 想法池整理
   │              │              │                  │              │
 边界确认      量级驱动        七算子扫描          四类视角        idea-pool.md
 维度轴定义    延迟判断                                            标记来源
              组合衍生
```

### P0 — 明确发散主题

确认三件事再开始发散：

1. **发散主题**：要扩展什么？（一句话能说清）
2. **边界**：发散范围、约束条件（哪些维度要扩，哪些不在范围内）
3. **问题空间维度轴**：定义 5-7 个评估维度（由 AI 提议，用户确认）

如果用户没明确主题，**主动追问**——主题不清做不出有用的发散。不要硬扩。

**维度轴适用性检查**：维度轴假设问题空间可以分解为独立维度。**不适用于高度耦合主题**（如"如何提升团队士气"——薪酬/认可/成长/工作生活平衡高度交织）。如果主题维度高度耦合，跳过维度轴，直接用 P1 自由发散 + P2a SCAMPER 补盲。

**维度轴定义**：

AI 根据主题提议 5-7 个维度（如"输入源/处理管线/输出形态/交互方式/存储策略"），用户确认或调整。同时根据主题特性判定 `problem_type`（deterministic / non_deterministic / mixed），写入 idea-pool.md frontmatter：

```yaml
---
topic: "{主题}"
problem_type: "deterministic | non_deterministic | mixed"
total_ideas: {N}
dimensions:
  - 维度1
  - 维度2
  - 维度3
  - ...
---
```

维度轴本身是发散的一部分——如果用户觉得 AI 提议的维度不合理，可以调整。中途发现某维度始终空白（连续 2 次暂停该维度 0%）或全部想法堆在一个维度（该维度 >80% 且其余维度均 <20%）时，允许重新定义维度轴。**调整时已分类想法按新维度重新归类，覆盖率按新轴重算。**

### P1 — 自由发散

**核心原则**：

- **量级驱动**：先追求数量，质量从数量里浮现。低于 15 个想法不算"扩开了"
- **延迟判断**（R2 在 AI 场景的定义）：P1/P2 阶段**不做**任何价值筛选、可行性判断、优先级排序、"这个不太现实"的过滤
- **疯狂想法保护**：明显"不切实际"的想法照样保留——后续收敛阶段会处理
- **想法组合（Hitchhiking）**：AI 主动在已有想法基础上衍生新想法，例如 "想法 #3 + 想法 #7 → 新想法 #N"

**关键协议（R4 切换入口）**：

每产出约 8-10 个想法后，**主动暂停并展示问题空间覆盖率**：

> **📊 问题空间覆盖率（累计 N 个）**
> <!-- 饱和度 = 想法数 ÷ 目标（默认 8，待校准），四舍五入到整十 -->
>
> | 维度 | 想法数(饱和度) | 状态 |
> |------|---------------|------|
> | 维度1 | 6 (80%) | 🔴 饱和 |
> | 维度2 | 4 (50%) | ⚪ 继续 |
> | 维度3 | 2 (30%) | 🟢 空白 |
> | 维度4 | 3 (40%) | 🟢 空白 |
> | ... | ... | ... |
>
> **下一步选项**：
> - 定向发散维度3/4（当前 ≤40%）
> - 避免维度1（已饱和）
> - 继续 P1 自由发散
> - 进入 P2a SCAMPER 结构化补盲

**决策规则**（按优先级排序，首条匹配即生效）：

| 条件 | 建议 |
|------|------|
| 所有维度 ≥ 60% **且**总数 ≥ 18（待校准） | 建议进入下一阶段 |
| 存在 ≤ 40% 维度 | 建议定向发散该维度 |
| 存在 > 80% 维度 | 标记饱和，提示不要再往这里扩 |
| 所有维度在 41%-59% | 继续自由发散，或切换到 SCAMPER 补盲 |

> ⚠️ **魔法数字待校准**：5-7 个维度、目标 8 个/维度、阈值 40%/60%/80%、总数 ≥ 18 均为初始估计值，需通过 3-5 次实战使用后校准。如果发现某个阈值频繁误判，记录到 BACKLOG。

不要默默继续。让用户基于数据控制节奏。

**想法不足处理**：P1 结束时如果不足 15 个，显式告知：

> 当前共 N 个想法，建议继续自由发散或切换到 SCAMPER 补盲来打开更多空间。

不要假装数量够了。

### P2a — SCAMPER 补盲

SCAMPER **是补盲工具不是主框架**。P1 已经铺开后，用七个算子作为 structured checklist 扫一遍，补 P1 漏掉的视角：

| 算子 | 含义 | 触发提问 |
|------|------|---------|
| **S** Substitute | 替代 | 哪个组件/角色/材料/流程可以被替换？ |
| **C** Combine | 组合 | 哪两个想法/功能/资源合并会产生新可能？ |
| **A** Adapt | 改造 | 别的领域有没有类似的解法可以借过来？ |
| **M** Modify/Magnify/Minimize | 修改/放大/缩小 | 把某个属性放大 10 倍或缩到极小会怎样？ |
| **P** Put to other use | 它用 | 现有的东西换个场景使用会怎样？ |
| **E** Eliminate | 去除 | 砍掉某个步骤/组件/约束会怎样？ |
| **R** Reverse/Rearrange | 反转/重排 | 倒过来做会怎样？顺序换一下会怎样？ |

**逐条扫描，每个算子至少产出 1-2 个新想法。仍然延迟判断。**

🔴 **P2a → P2b 切换入口**：

SCAMPER 扫描完毕后，**主动展示问题空间覆盖率全景并询问**：

> **📊 问题空间覆盖率全景（累计 N 个）**
>
> | 维度 | P1 | P2a | 累计(饱和度) | 状态 |
> |------|-----|-----|-------------|------|
> | 维度1 | 6 | 2 | 8 (100%) | 🔴 饱和 |
> | 维度2 | 4 | 3 | 7 (90%) | 🔴 饱和 |
> | 维度3 | 2 | 1 | 3 (40%) | 🟢 空白 |
> | 维度4 | 3 | 2 | 5 (60%) | ⚪ 继续 |
> | ... | ... | ... | ... | ... |
>
> SCAMPER 算子覆盖：S×n / C×n / A×n / M×n / P×n / E×n / R×n（共 7/7）
>
> **下一步选项**：
> - 进入 P2b 视角轮换（重点覆盖维度3）
> - 直接整理为想法池
> - 回到 P1 继续发散

**P2a 分阶段解读**：如果某维度 P1 已饱和但 P2a 零贡献，说明 SCAMPER 未能打开该维度的新视角，可跳过该维度的后续补盲。

### P2b — 利益相关方视角轮换

切到不同视角再扫一遍，补 P2a 之后剩下的盲区：

| 视角 | 关注点 |
|------|-------|
| 👤 用户/终端使用者 | 真正在用的人在乎什么？日常痛点是什么？ |
| 🛠 技术/实现者 | 做出来的人怎么看？实现路径上有什么变体？ |
| ⚔ 竞争者/对手 | 对手会怎么打这个主题？他们的解法是什么？ |
| 🌪 极端/边缘用户 | 5% 边缘场景的需求是什么？极端条件下怎么用？ |

每个视角至少 2-3 个想法。**仍然延迟判断**。

🔴 **P2b → P3 确认入口**：

视角轮换完毕后，**主动确认**：

> 发散阶段完成。共 N 个想法（P1: n1, P2a: n2, P2b: n3）。确认整理为 idea-pool.md？
>
> 选项：(1) 确认整理 (2) 回到某个阶段继续发散

### P3 — 想法池整理

把 P1 + P2a + P2b 全部想法汇总，**标记来源**，写入 `idea-pool.md`。

**YAML frontmatter 四字段**：

```yaml
---
topic: "发散主题"
problem_type: "deterministic | non_deterministic | mixed"
total_ideas: 42
dimensions:
  - 维度1
  - 维度2
  - 维度3
  - ...
---
```

**正文结构**：

```markdown
---
topic: "{发散主题}"
problem_type: "deterministic | non_deterministic | mixed"
total_ideas: {N}
dimensions:
  - 维度1
  - 维度2
  - 维度3
  - ...
---

# 想法池：{主题}

## P1 自由发散（{n1} 个）

1. {想法描述} `[P1]`
2. {想法描述} `[P1]` `[组合: #1 + #N]`
...

## P2a SCAMPER 补盲（{n2} 个）

### S Substitute
- {想法} `[P2a-S]`
### C Combine
- {想法} `[P2a-C]`
...

## P2b 视角轮换（{n3} 个）

### 用户视角
- {想法} `[P2b-用户]`
### 技术视角
- {想法} `[P2b-技术]`
### 竞争者视角
- {想法} `[P2b-竞争]`
### 边缘视角
- {想法} `[P2b-边缘]`
```

每条想法**不带评分、不排序、不标"推荐"**——这些是 hegel 的工作。

输出文件路径建议：`docs/osborn-pools/{topic-slug}-{date}.md`。

## 不做的事

- **不做三模式**（Quick/Deep/Investment）——发散工具不需要"分析深度控制"
- **不做 SubAgent 并行**——V2 是纯对话协议
- **不做交接协议**——想法池以 Markdown 文件形式自然传递给 hegel
- **不做评分/优先级/可行性筛选**——这是 hegel 的工作
- **不做需求澄清**——这是 ce-brainstorm 的工作
- **不做 PRD 内嵌方案讨论**——这是 liam-prd-writer 的能力
- 不含脚本——纯 SKILL.md 模式

## 边界条件与 Fallback

| 场景 | 触发条件 | 处理动作 |
|------|---------|----------|
| 用户中途退出 | 任意阶段用户说"够了"/"停"/"就这样" | 直接进 P3，整理当前已有想法为 idea-pool.md |
| 用户跳过 P2 | 用户说"不需要 SCAMPER"/"不用视角轮换" | 允许 P1→P3 直连，idea-pool.md 中仅含 P1 段落 |
| 主题无法发散 | P0 追问 2 轮后主题仍模糊 | 建议先用 ce-brainstorm 澄清需求，再来 osborn 发散 |
| 想法数量远不足 | P1 结束 < 10 个 | 显式告知并建议：继续发散 / 切换 SCAMPER / 调整主题范围 |
| 维度轴失效 | 连续 2 次暂停某维度 0%，或单维度 >80% 且其余均 <20% | 提议重新定义维度轴，用户确认后按新轴重算覆盖率 |

## 与下游 skill 的衔接

```
osborn (idea-pool.md + dimensions 字段)
   │
   ▼
hegel --target idea-pool.md --lens ideation
   │（盲区识别 + 假共识检查 + 收敛到候选方案）
   │（hegel 读取 dimensions 字段，用于维度级收敛优先级判断）
   ▼
bayes --claim "候选方案 X 站得住"
   │（外部数据验证）
   ▼
feynman（产出审查）
   │
   ▼
debono（价值回溯——找回被误杀的东西）
```

idea-pool.md 是 hegel 的 S2 输入。hegel 会把 P1/P2a/P2b 三段视为已有 claims，跑 rigor gate 和后续管线。`dimensions` 字段供 hegel 做维度级收敛分析。

## 反模式（不要这样做）

| 坑 | 表现 | 怎么防 |
|----|------|--------|
| 提前收敛 | P1 才出 5 个想法就停 | 强制最少 15 个 |
| 偷偷筛选 | 心里觉得"这个不行"就不写出来 | 写出来再说，不评判 |
| 视角偏斜 | P2b 只用了 1-2 个视角 | 四个视角必须全跑 |
| 跳过 SCAMPER | 觉得 P1 已经够了直接进 P3 | 七算子至少各扫一遍 |
| 自动接续 | 不询问用户就一直发散 | 8-10 个后必须主动询问（R4） |
| 在 P3 做评分 | 想法池里加"推荐度" | 评分是 hegel 的事，不是这里 |

---

## 目录结构说明

```
osborn/
├── SKILL.md                    # 主文件（你正在读的）
├── .scope                      # 作用域声明
├── evals/                      # 非标准目录：开发测试用例
│   └── dev-cases.json          # 6 个测试场景（2 happy_path + 3 edge_case + 1 negative）
└── references/                 # 对比参考材料（从主文件拆分）
    ├── comparisons.md          # 太虚五转分工表 + 与 ce-brainstorm 对比表
    └── complete-run-benchmark.md  # 完整发散基准案例（57 个想法的实际产出）
```

**evals/** 存放开发阶段的测试用例，用于验证 skill 行为是否符合预期。每个 case 包含 prompt、expected 产出、type（happy_path / edge_case / negative）和 check_points。这些用例不是自动化测试，而是人工验证时的检查清单。

**references/** 存放从 SKILL.md 主文件拆分的对比表和参考材料，降低主文件的表格密度，同时保留完整信息供需要时查阅。

---

## 版本演进

| 版本 | 日期 | 变更 |
|------|------|------|
| V1.0 | 2026-06-11 | 首版设计：P0→P1→P2a→P2b→P3 流程 + 六项核心能力 |
| V1.1 | 2026-06-11 | Darwin+Godel 优化：检查点补齐(🔴×2) + 边界条件 fallback + 触发词收窄 + 否定路由 + metadata 治理字段 |
| V1.2 | 2026-06-12 | R1 优化版 |
| V1.2.1 | 2026-06-22 | 表格拆分(comparisons→references/) + description主动触发增强 + allowed-tools/compatibility字段 + evals目录说明 |
| V1.2.2 | 2026-06-25 | 想法演化分析：暂停点展示边际贡献分布（🟦新方向/🟩深化/🟨组合变异/🟥过度工程），只呈现数据不替代判断 |
| V2.0 | 2026-06-25 | 10 次实战验证固化（antifragile-discovery → anthropic-adoption），流程稳定、产出质量一致，V1→V2 升级 |
| V2.1 | 2026-06-25 | 问题空间覆盖率替代边际贡献分析（🟦🟩🟨🟥）：暂停点从"想法间拓扑关系"改为"维度×饱和度"驱动决策 |
| **V2.1.1** | **2026-06-25** | **费曼审查修复**：① 概述增加维度轴直觉解释（地图+坐标轴+探索进度）② P0 显式声明维度轴不适用于高度耦合主题 ③ P1/P2a 暂停点增加精简版 prose 决策建议（只列行动选项）④ P2a 恢复分阶段 breakdown（P1/P2a/累计）+ 解读说明 ⑤ 魔法数字标记"待校准" |

> ⚠️ 原 V1.3/V1.2.3 命名违反 semver（V1.3 先于 V1.2.3），已修正为 V1.2.1/V1.2.2。
